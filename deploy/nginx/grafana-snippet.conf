# 片段：并入本机 / 网关 Nginx。运营后台同域反代 Grafana + Auth Proxy。
# 使用前请先能校验运营后台登录（此处示例用 cookie hugme_admin_token 非空；生产应校验 JWT）。
#
# selfpraise-admin iframe 示例：
#   /grafana/d/hugme-http-requests/hugme-http-requests?kiosk&theme=light

# map $cookie_hugme_admin_token $grafana_user {
#     default "";
#     "~.+"   "ops-viewer";
# }

# location /grafana/ {
#     # 未登录则拒绝（生产改为跳转后台登录页）
#     if ($grafana_user = "") {
#         return 401;
#     }
#
#     proxy_pass http://127.0.0.1:3001/;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#
#     # 禁止客户端伪造；仅网关注入
#     proxy_set_header X-WEBAUTH-USER $grafana_user;
# }
